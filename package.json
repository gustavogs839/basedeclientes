{
  "name": "base-clientes-lopes",
  "version": "1.0.0",
  "private": true,
  "description": "Cadastro de clientes com botao de WhatsApp (Firebase Hosting + Firestore)",
  "scripts": {
    "serve": "npx firebase-tools emulators:start --only hosting,firestore",
    "deploy": "npx firebase-tools deploy"
  }
}
